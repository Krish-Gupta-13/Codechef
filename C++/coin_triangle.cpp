#include <bits/stdc++.h>
using namespace std;

int minimum_operations(vector<int>& arr, int n){
		
}
int main(){
	vector<int> arr = {3,4,3};
	int n = arr.size();
	cout << minimum_operations(arr, n);
	return 0;
}